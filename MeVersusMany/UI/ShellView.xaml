<UserControl x:Class="MeVersusMany.UI.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MeVersusMany">
    <DockPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="Auto" Width="Auto">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Menu">
                <MenuItem x:Name="ExitApplication" Header="Exit"/>
            </MenuItem>
        </Menu>
        <DockPanel>
            <ContentControl x:Name="PlayerStats" DockPanel.Dock="Left"/>
            <ContentControl x:Name="OverallStats" DockPanel.Dock="Right"/>
            <ContentControl x:Name="Ranking"/>
        </DockPanel>
    </DockPanel>
</UserControl>
